overwrite: true
schema: "tests/schema.graphql"
generates:
  "tests/types.ts":
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
    config:
      # this allows for partial resolution of fields
      # in resolvers
      defaultMapper: Partial<{T}>
      useIndexSignature: true
